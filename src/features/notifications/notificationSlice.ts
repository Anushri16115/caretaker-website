export const notificationInitialState = { items: [] }

export default function notificationReducer(state = notificationInitialState, action: any) {
  switch (action.type) {
    default:
      return state
  }
}
