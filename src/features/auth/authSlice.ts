// Minimal placeholder for auth slice
export const authInitialState = { user: null }

export default function authReducer(state = authInitialState, action: any) {
  switch (action.type) {
    default:
      return state
  }
}
